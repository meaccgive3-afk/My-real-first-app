import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'app.sakinah.prayer',
  appName: 'سَكِينة',
  webDir: 'dist',
  backgroundColor: '#16201c',
  android: {
    backgroundColor: '#16201c',
  },
}

export default config
